// routes for items


const express = require('express');
const router  = express.Router();

// get items
router.get('/', (req, res) => {});


//get items/:itemid
router.get('/:id', (req, res) => {});


module.exports = router;
